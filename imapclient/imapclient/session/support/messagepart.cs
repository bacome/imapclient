using System;

namespace work.bacome.imapclient
{
    public partial class cIMAPClient
    {
        private partial class cSession
        {
            /*
            private class cMessagePartx
            {
                public readonly uint Origin;
                public readonly uint Length;

                public cMessagePart(uint pOrigin, uint pLength)
                {
                    if (pLength == 0) throw new ArgumentOutOfRangeException(nameof(pLength));
                    Origin = pOrigin;
                    Length = pLength;
                }

                public override string ToString() => $"{nameof(cMessagePart)}({Origin},{Length})";
            } */
        }
    }
}